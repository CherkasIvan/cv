export interface IEducationPlaceName {
    // id: string need to be added when we will have fdb
    name: string;
    link: string;
    address: string;
    phone: string;
    grade?: string;
    certificatePath?: string;
    certificateLink?: string;
}
